type ToolCardProps = {
  title: string;
  description: string;
  image: string;
  color: string | null;
  isBackgroundColor: boolean;
};

// Have the Id and a numberic and change color and bg according to it

const ToolCard = () => {};

export default ToolCard;
